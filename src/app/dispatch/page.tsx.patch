--- /home/jakob/.openclaw/workspace/office-angel-web/src/app/dispatch/page.tsx
+++ /home/jakob/.openclaw/workspace/office-angel-web/src/app/dispatch/page.tsx
@@ -434,13 +434,23 @@
   }, [techs.length]);
 
   return (
-    <div className="max-w-[1800px] mx-auto p-4 md:p-6 min-h-[calc(100dvh-3.5rem)] lg:h-[calc(100vh-2rem)] flex flex-col bg-gray-50/50">
+    <div className="max-w-[1800px] mx-auto p-4 md:p-6 min-h-[calc(100dvh-3.5rem)] lg:h-[calc(100vh-2rem)] flex flex-col bg-[#F9FAFB]">
       {/* Job Ticket Slide-over */}
       {ticketOpen && (
         <div className="fixed inset-0 z-50 flex justify-end">
           <div className="absolute inset-0 bg-black/30 backdrop-blur-sm" onClick={closeTicket} />
-          <div className="relative z-10 w-full max-w-lg bg-white shadow-2xl flex flex-col h-full overflow-hidden">
+          <div className="relative z-10 w-full max-w-lg bg-white shadow-[0_0_40px_rgba(0,0,0,0.1)] flex flex-col h-full overflow-hidden border-l border-gray-100">
             <div className="p-6 border-b border-gray-200 bg-gray-50 flex items-start justify-between">
               <div>
-                <p className="text-xs text-gray-500 uppercase font-bold tracking-wider">Job Ticket</p>
-                <h2 className="text-xl font-bold text-gray-900 mt-1">
+                <div className="flex items-center gap-2 mb-1">
+                  <div className="h-2 w-2 rounded-full bg-blue-500 animate-pulse"></div>
+                  <p className="text-[10px] text-gray-500 uppercase font-bold tracking-widest">Live Dispatch Ticket</p>
+                </div>
+                <h2 className="text-2xl font-black text-gray-900 tracking-tight">
                   {ticketLoading ? 'Loading…' : (ticket?.title || 'Untitled Job')}
                 </h2>
                 {ticket?.customers?.phone_number && (
-                  <p className="text-sm text-gray-600 mt-1">
-                    {ticket.customers.first_name ? `${ticket.customers.first_name} ${ticket.customers.last_name || ''}`.trim() : 'Customer'} • {ticket.customers.phone_number}
+                  <p className="text-sm text-gray-500 mt-1.5 font-medium flex items-center gap-2">
+                    <User size={14} />
+                    {ticket.customers.first_name ? `${ticket.customers.first_name} ${ticket.customers.last_name || ''}`.trim() : 'Customer'} <span className="text-gray-300">|</span> {ticket.customers.phone_number}
                   </p>
                 )}
               </div>
-              <button onClick={closeTicket} className="text-sm font-semibold text-gray-600 hover:text-gray-900">
+              <button onClick={closeTicket} className="text-xs font-bold text-gray-400 hover:text-gray-900 bg-gray-100 hover:bg-gray-200 px-3 py-1.5 rounded-full transition-colors">
                 Close
               </button>
             </div>
 
-            <div className="flex-1 overflow-y-auto p-6 space-y-5">
+            <div className="flex-1 overflow-y-auto p-6 space-y-6 bg-[#FDFDFD]">
               {ticketLoading ? (
-                <div className="text-sm text-gray-500">Fetching job details…</div>
+                <div className="flex flex-col items-center justify-center h-32 space-y-3">
+                  <div className="w-6 h-6 border-2 border-blue-500 border-t-transparent rounded-full animate-spin"></div>
+                  <div className="text-sm font-medium text-gray-400">Syncing with field...</div>
+                </div>
               ) : !ticket ? (
                 <div className="text-sm text-gray-500">Could not load job.</div>
               ) : (
                 <>
-                  <div className="bg-white border border-gray-200 rounded-xl p-4">
+                  <div className="bg-white border border-gray-100 shadow-sm rounded-2xl p-5">
                     <div className="flex items-center justify-between">
-                      <span className="text-xs font-bold text-gray-500 uppercase">Status</span>
+                      <span className="text-[10px] font-bold text-gray-400 uppercase tracking-widest">Status</span>
                       <span className={`text-xs font-bold px-2 py-1 rounded border ${statusPill(ticket.status)}`}>{ticket.status || 'Lead'}</span>
                     </div>
                     {ticket.address && (
-                      <p className="text-sm text-gray-800 mt-3 flex items-start gap-2">
-                        <MapPin size={16} className="text-gray-400 mt-0.5" />
+                      <p className="text-[15px] font-medium text-gray-800 mt-4 flex items-start gap-3 p-3 bg-gray-50 rounded-xl">
+                        <MapPin size={18} className="text-blue-500 mt-0.5" />
                         <span>{ticket.address}</span>
                       </p>
                     )}
